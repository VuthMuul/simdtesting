#pragma once

#include <cmath>

const int VEC_LEN = 100'000'000;
const int SEED_A = 5318008;
const int SEED_B = 1337;
const float TOLERANCE = 0.000001;
const float ITER_TOL  = 0.000001;
const int SAMPLE_COUNT = 10;
extern int ITER_DEPTH;
const int MAX_ITER = 15;

const float dist2 = sqrt(1/0.2);
const float dist4 = sqrt(1/0.4);
const float dist6 = sqrt(1/0.6);
const float dist8 = sqrt(1/0.8);

const float dist5 = sqrt(1/0.5);
